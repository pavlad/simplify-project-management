var new_task = $("<%= j render "projects/task", task: @task %>").hide();
$('#task-list .list').append(new_task);
new_task.fadeIn();
$('#task-form').find('input').val('');
$('#task-form').find('select').val('');
